# SmartStock 智能股票交易辅助系统 — 产品需求文档 (PRD)

> 版本: 1.0
> 生成日期: 2026-05-12
> 基于: 系统源码反向生成

---

## 1. 产品概述

### 1.1 产品定位

SmartStock 是一款面向 A 股个人投资者的**盘中实时交易辅助工具**，将《顶级股票交易系统》的规则化交易体系转化为可交互的 Web 应用。系统不提供自动交易，而是作为「纪律执行器」，帮助用户在盘中完成：判断大盘环境 → 选股筛选 → 仓位计算 → 交易记录 → 复盘分析 的完整闭环。

### 1.2 目标用户

- 有一定交易经验但缺乏纪律性的 A 股个人投资者
- 希望将主观交易转化为系统化交易的散户
- 已阅读或正在学习结构化交易规则的进阶投资者

### 1.3 核心价值

| 维度 | 价值 |
|------|------|
| 纪律 | 强制执行交易前检查清单、止损规则、仓位上限 |
| 效率 | 一站式完成从大盘判断到下单决策的全流程 |
| 复盘 | 自动统计胜率/盈亏比，识别违规模式和连续亏损 |
| 学习 | 策略速查页面随时回顾规则，降低决策犹豫 |

### 1.4 技术架构

| 层 | 技术 | 说明 |
|----|------|------|
| 前端 | Vue 3.5 + Vite 8 + Vue Router 4 + Pinia 2 | SPA，Composition API |
| 后端 | Koa 2 + @koa/router + @koa/cors | 纯 API 代理层，无数据库 |
| 数据源 | 东方财富公开 API | 通过后端代理避免 CORS |
| 持久化 | localStorage | 用户数据全部本地存储 |
| 部署 | 静态部署 | `vite build` + Node 起后端即可 |

---

## 2. 页面结构与导航

### 2.1 全局导航

顶部固定导航栏（高度 52px），包含 Logo「SmartStock」与 6 个页面入口：

| 路由 | 名称 | 图标文字 |
|------|------|----------|
| `/` | 大盘状态 | Dashboard |
| `/watchlist` | 股票池 | Watchlist |
| `/screener` | 选股筛选 | Screener |
| `/position` | 仓位计算 | Position |
| `/journal` | 交易日志 | Journal |
| `/guide` | 策略速查 | Guide |

导航栏 sticky 置顶，激活态高亮蓝色背景，移动端支持横向滚动。

### 2.2 全局样式

- **主题**: 暗色系（深海军蓝背景 `#0f172a`）
- **字体**: `-apple-system, "PingFang SC", "Microsoft YaHei", sans-serif`
- **响应式**: 768px 断点，栅格从多列折叠为单列
- **滚动条**: 全局 thin 样式，6px 宽，半透明
- **基础单位**: 字号 14px，行高 1.6，圆角 8px

---

## 3. 功能模块详细需求

### 3.1 大盘状态 (Dashboard)

**路由**: `/`
**用途**: 盘前/盘中判断当前市场环境，决定是否开仓及仓位上限

#### 3.1.1 六维判据表

系统自动采集 6 个维度数据，逐条判断牛/震/熊信号：

| 维度 | 数据来源 | 牛市信号 | 熊市信号 |
|------|----------|----------|----------|
| 均线系统 | MA20/60/120 多空排列 | 多头排列 | 空头排列 |
| 价格 vs MA60 | 指数收盘价与 MA60 关系 | 站上 MA60 且 MA60 拐头向上 | 跌破 MA60 |
| 创新高/低 | 近 20 日新高新低家数 | 新高/新低 > 2 | 新低/新高 > 2 |
| 涨跌家数 | 沪深两市实时涨跌统计 | 上涨/下跌 > 2 | 下跌/上涨 > 2 |
| 成交量趋势 | 5 日均量 vs 20 日均量 | > 1.0 放量 | — |
| 北向资金 | 近 5 日净流入 | ≥ 4 日净流入 | ≤ 1 日净流入 |

**后端接口**:
- `GET /api/market/indices` — 上证/深证/创业板指数 + 120 日 K 线 + MA20/60/120
- `GET /api/market/breadth` — 沪深涨/跌/平家数
- `GET /api/market/northbound` — 近 5 日北向资金净流入

#### 3.1.2 综合判定

根据六维信号数量综合判定市场状态：

| 状态 | 条件 | 建议总仓位 | 推荐策略 |
|------|------|------------|----------|
| 牛市 | ≥ 4 个牛信号 | 80-100% | 趋势突破 |
| 偏多 | ≥ 3 个牛信号 | 50-70% | 回调买入 |
| 震荡 | 其他 | 50-70% | 回调买入 |
| 偏空 | ≥ 3 个熊信号 | 20-40% | 仅观望 |
| 熊市 | ≥ 4 个熊信号 | 0-20% | 空仓 |

需 ≥ 3 个信号方向一致才确认（`confirmed` 字段）。

#### 3.1.3 长窗口速判

3 项自动检测条件，满足即提示多头窗口开启：
1. 指数收盘价 > MA60
2. MA60 连续 3 天拐头向上
3. 涨跌比 > 1.5

#### 3.1.4 交易前检查清单

9 项必检项（来源 `PRE_TRADE_CHECKLIST`），以复选框形式呈现：
1. 大盘环境是否适合交易？
2. 该股是否通过排雷清单？
3. 基本面是否符合标准？
4. 是否有明确的买入信号？
5. 止损位是否已设定？（用 ATR 计算）
6. 盈亏比是否 ≥ 2:1？
7. 仓位是否在风险预算内？（≤ 25%）
8. 是否在冷静状态下决策？
9. 是否已设条件单/提醒？

---

### 3.2 股票池 (Watchlist)

**路由**: `/watchlist`
**用途**: 自选股管理、实时行情查看、个股走势分析

#### 3.2.1 页面布局

全屏高度布局（`height: calc(100vh - 52px)`），无页面级滚动：

```
┌──────────────────────────────────────────────┐
│  上证分时 │ 深证分时 │ 创业板分时    (顶部指数栏) │
├──────────┬───────────────────────────────────┤
│ 侧边栏   │         详情主区域                  │
│ (320px)  │                                   │
│ · 搜索   │   · 大走势图 (6 种周期)            │
│ · 股票列表│   · 统计数据网格                    │
│ · 微型图 │   · 操作按钮                       │
│          │                                   │
│ 独立滚动 │         独立滚动                    │
└──────────┴───────────────────────────────────┘
```

#### 3.2.2 顶部指数栏

- 展示上证指数(000001)、深证成指(399001)、创业板指(399006)
- 每张卡片包含：指数名称、当前值、涨跌幅（着色）、分时走势迷你图（高度 68px，自动占满宽度）
- 分时图带昨收虚线参考线
- 数据来源: `GET /api/market/indices/intraday`

#### 3.2.3 侧边栏 — 搜索与自选列表

**搜索功能**:
- 输入框支持按代码、名称、拼音首字母搜索
- 300ms 防抖，调用 `GET /api/stock/search?kw=`
- 下拉建议列表，过滤仅显示 A 股（`Classify === 'AStock'`）
- 最多显示 6 条建议
- 点击建议添加到自选列表

**自选股票列表**:
- 每行显示：股票名称、代码、当前价、涨跌幅（绿涨红跌）
- 迷你分时走势图（优先使用当日分时数据，无则用近 20 日 K 线）
- 点击选中 → 右侧展示详情
- 数据每 30 秒自动刷新（`GET /api/stock/batch/quotes`）
- 自选列表持久化到 localStorage

#### 3.2.4 详情主区域

**走势图**:
- Canvas 渲染，支持 Retina (DPR) 高清
- 6 种周期 Tab: 1日 | 1周 | 1月 | 3月 | 5年 | 全部
- 1 日: 分时数据，带昨收虚线
- 1 周/1 月/3 月: 日 K 线切片
- 5 年: 1200 日 K 线数据
- 全部: 1200 日 K 线
- 自动宽度，响应式 ResizeObserver
- 带面积渐变填充、高/低价格标注

**统计数据网格** (两列):
- 日内范围（最高/最低）
- 区间范围（期间最高/最低）
- 市盈率 (PE)
- 市净率 (PB)
- 总市值
- 涨跌幅
- 区间涨跌幅

**操作按钮**:
- 「计算仓位」→ 跳转 `/position?code=xxx`
- 「移除」→ 从自选列表删除

**后端接口**:
- `GET /api/stock/:code/kline` — 120 日 K 线
- `GET /api/stock/:code/kline5y` — ~5 年 K 线
- `GET /api/stock/:code/intraday` — 当日分时
- `GET /api/stock/:code/basic` — PE/PB/市值等基本面

---

### 3.3 选股筛选 (Screener)

**路由**: `/screener`
**用途**: 四层漏斗式选股，生成东方财富 App/PC 可用的筛选条件

#### 3.3.1 第一层：排雷（10 项）

排除有重大风险的股票，6 项自动检测 + 4 项需人工查阅 F10：

| # | 条件 | 检测方式 |
|---|------|----------|
| 1 | 非 ST | 自动 |
| 2 | 非停牌 | 自动 |
| 3 | 审计意见：标准无保留 | 自动 |
| 4 | 商誉/净资产 < 30% | 自动 |
| 5 | 大股东质押 < 60% | 自动 |
| 6 | 上市天数 > 250 天 | 自动 |
| 7 | 关联交易/营收 < 40% | F10 人工 |
| 8 | 无存贷双高 | F10 人工 |
| 9 | 董监高减持 < 5% | F10 人工 |
| 10 | 无重大诉讼/处罚 | F10 人工 |

**规则**: 至少勾选 5 项才能继续下一层。

#### 3.3.2 第二层：基本面（7 参数）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| ROE | ≥ 12% | 净资产收益率 |
| 营收增速 | ≥ 10% | 同比 |
| 利润增速 | ≥ 10% | 同比 |
| 资产负债率 | ≤ 60% | |
| 经营现金流 | 正值 | |
| PE 区间 | 5-40 | 排除极端估值 |
| 最小市值 | 30 亿 | 排除微盘股 |

#### 3.3.3 第三层：景气度（3 选 1）

| 选项 | 含义 |
|------|------|
| 机构增持 | 机构持股↑ + 北向持股↑ + 主力净流入 |
| 业绩超预期 | 净利润增速 > 30% + 营收增速 > 20% + 研报 ≥ 3 |
| 龙虎榜强势 | 近 3 日上榜 + 买方机构多 + 换手率 3-20% + 流通市值 > 30 亿 |

#### 3.3.4 第四层：技术信号（3 选 1）

| 策略 | 条件 |
|------|------|
| 趋势突破 | 放量突破 20 日高点 + MACD 金叉 + MACD 柱 > 0 + MA20↑ + MA60↑ |
| 回调买入 | 股价 > MA60 且 MA60↑ + 缩量回调至 MA20 ± 2% + 量缩至 20 日最低 |
| 底部右侧确认 | 从高点跌 > 40% + 缩量后放量 + RSI < 30 拐头 + MACD 金叉 |

#### 3.3.5 输出

根据勾选条件自动生成：
1. **自然语言筛选语句** — 可直接复制到东方财富 App「一句话选股」
2. **PC 公式代码** — 可直接粘贴到东方财富 PC 端「公式编辑器」
3. **F10 提醒** — 标注仍需人工查阅的风险项

---

### 3.4 仓位计算 (Position)

**路由**: `/position`
**用途**: 基于 ATR 的动态仓位计算、持仓管理、行业集中度监控

#### 3.4.1 ATR 动态仓位计算器

**输入**:
- 股票代码（带「获取 ATR」按钮，自动调用 K 线接口计算 ATR(14)）
- 买入价
- 目标价
- 策略选择（趋势突破 / 回调买入 / 底部右侧确认）

**自动计算**:

止损价 = 买入价 − N × ATR (N 由策略决定)

| 策略 | ATR 乘数 N | 回撤比例 | 建议持有期 |
|------|-----------|---------|-----------|
| 趋势突破 | 1.5 | 8% | 1-4 周 |
| 回调买入 | 2.0 | 15% | 2-8 周 |
| 底部右侧确认 | 3.0 | 20% | 1-6 月 |

仓位 = min(风险预算 / 止损比例, 总资金 × 25%)

其中:
- 风险预算 = 总资金 × 2%（单笔最大亏损）
- 25% 为单股仓位上限

盈亏比 = (目标价 − 买入价) / (买入价 − 止损价)

**展示**:
- 止损价、建议仓位（万元）、仓位占比
- 盈亏比数值（建议 ≥ 2:1）
- 止损/止盈可视化条形图

#### 3.4.2 行业集中度

- 按行业分组统计持仓占比
- 某行业 > 30% 时发出警告
- 可视化柱状图

#### 3.4.3 持仓管理表

| 字段 | 说明 |
|------|------|
| 代码 | 股票代码 |
| 行业 | 所属行业 |
| 买价 | 买入价格 |
| 止损价 | 初始止损 |
| 跟踪止盈 | 动态止损价（只能上移，不能下调） |
| 仓位 | 持仓金额 |
| 策略 | 策略名称 |
| 日期 | 开仓日期 |

**操作**:
- 添加持仓：从计算器结果直接添加
- 更新跟踪止盈：仅允许上调
- 删除持仓
- 总资金可配置（默认 100 万），持久化到 localStorage

#### 3.4.4 策略参数参考表

展示三种策略的完整参数对照：ATR 乘数、回撤比例、建议持有期、时间止损规则。

---

### 3.5 交易日志 (Journal)

**路由**: `/journal`
**用途**: 记录每笔交易的完整生命周期，自动统计绩效，辅助复盘改进

#### 3.5.1 页面结构

三个 Tab 页切换：

| Tab | 功能 |
|-----|------|
| 交易记录 | 新增/平仓/查看交易 |
| 绩效统计 | 胜率、盈亏比、月度曲线 |
| 复盘分析 | 规则违反统计、系统警告 |

#### 3.5.2 新增交易表单

**展开/收起**设计，包含以下字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| 股票 | 搜索选择 | 复用股票搜索 API |
| 买入价 | 数字 | |
| 数量 | 整数 | |
| 策略 | 下拉 | 趋势突破/回调买入/底部右侧确认 |
| 市场环境 | 下拉 | 牛市/偏多/震荡/偏空/熊市 |
| 行业 | 文本 | 如「半导体」 |
| ATR | 只读 | 点击「获取 ATR」按钮自动填充 |
| 止损价 | 数字 | 可自动计算也可手动输入 |
| 目标价 | 数字 | 可自动计算也可手动输入 |
| 交易前检查 | 9 项复选框 | 来源 PRE_TRADE_CHECKLIST |

**盈亏比提示**: 实时显示当前买价/止损/目标计算出的盈亏比，< 2:1 时红色警告。

**ATR 自动计算流程**:
1. 用户输入股票代码和买入价
2. 点击「获取 ATR」→ 调用 `GET /api/stock/:code/kline`
3. 取最近 15 根 K 线，计算 True Range 序列
4. ATR = SMA(TR, 14)
5. 自动填充止损价 = 买入价 − N × ATR
6. 自动填充目标价 = 买入价 + N × ATR × 2

#### 3.5.3 持仓列表

展示所有 `status === 'open'` 的交易：

| 列 | 内容 |
|----|------|
| 股票 | 名称 + 代码 |
| 策略 | 策略名称 |
| 买价 | 买入价 |
| 止损 | 止损价（红色） |
| 目标 | 目标价（绿色） |
| 数量 | 持仓数量 |
| 日期 | 开仓日期 |
| 操作 | 平仓 / 删除 |

#### 3.5.4 平仓表单（弹窗）

点击「平仓」弹出模态框：

| 字段 | 类型 | 说明 |
|------|------|------|
| 卖出价 | 数字 | |
| 卖出原因 | 下拉 | 触发止损 / 到达目标 / 跟踪止盈 / 时间止损 / 逻辑止损 / 手动 |
| 情绪评分 | 1-5 按钮 | 1=极差, 5=极佳 |
| 执行评分 | 1-5 按钮 | 1-5 分 |
| 规则违反 | 7 项复选框 | 对应 7 条铁律 |
| 问题标签 | 5 项复选框 | 执行失误 / 时机错误 / 选股错误 / 环境不匹配 / 情绪驱动 |
| 复盘备注 | 多行文本 | 自由记录 |

**自动计算**:
- 盈亏 = (卖出价 − 买入价) × 数量
- 盈亏比 = (卖出价 − 买入价) / (买入价 − 止损价)
- 盈亏百分比 = (卖出价 − 买入价) / 买入价 × 100%

#### 3.5.5 已平仓列表

| 列 | 内容 |
|----|------|
| 股票 | 名称 + 代码 |
| 策略 | 策略名称 |
| 买/卖价 | 买入价 → 卖出价 |
| 盈亏 | 金额 + 百分比（盈利绿色，亏损红色） |
| 盈亏比 | actualRR 数值 |
| 卖出原因 | 止损/目标/手动等 |
| 日期 | 卖出日期 |
| 操作 | 展开「复盘」查看详情 |

#### 3.5.6 绩效统计

**摘要卡片** (6 宫格):

| 指标 | 计算方式 |
|------|----------|
| 胜率 | 盈利交易数 / 总平仓数 × 100% |
| 盈亏比 | 总盈利 / 总亏损 |
| 总盈亏 | 所有平仓交易 P&L 之和 |
| 平均盈利 | 盈利交易平均 P&L% |
| 平均亏损 | 亏损交易平均 P&L% |
| 交易次数 | 已平仓交易总数 |

**月度权益曲线**:
- Sparkline 折线图，横轴月份，纵轴累计盈亏
- 下方表格列出每月盈亏明细
- 数据按 YYYY-MM 分组

**策略对比表**:

| 列 | 内容 |
|----|------|
| 策略 | 策略名称 |
| 交易数 | 该策略的平仓交易数 |
| 胜率 | 该策略的胜率 |
| 总盈亏 | 该策略的累计 P&L |

#### 3.5.7 复盘分析

**系统警告**:
- 连续 ≥ 5 次止损 → 红色警告横幅
- 提示：暂停交易，复盘检讨

**规则违反统计**:
- 7 条铁律各自被违反的次数
- 来源: IRON_RULES
  1. 止损不可撤销
  2. 不追涨杀跌
  3. 不逆势满仓
  4. 不在亏损仓位加仓
  5. 不过度交易
  6. 不碰不懂的票
  7. 不临时起意

**问题分析**:
- 5 类问题标签各自出现频次
  - 执行失误 / 时机错误 / 选股错误 / 环境不匹配 / 情绪驱动

**复盘记录列表**:
- 有填写复盘备注的已平仓交易
- 展示：股票名、盈亏金额、日期、卖出原因、情绪评分、执行评分、备注内容

---

### 3.6 策略速查 (Guide)

**路由**: `/guide`
**用途**: 随时可查的交易规则参考手册

#### 3.6.1 页面布局

左侧固定导航（140px）+ 右侧滚动内容区，共 9 个章节。

#### 3.6.2 章节内容

| 章节 | 内容 |
|------|------|
| 想买入 | 6 节点顺序决策树，通过是/否引导到正确操作 |
| 买入策略 | 3 种策略详细入场规则 + 时间止损 + 适用市场 + 汇总对照表 |
| 要卖出 | 4 级卖出优先级、ATR 止损公式、跟踪止盈公式（只上移不下调）、其他止损类型 |
| 不知仓位 | 总仓位表（按市场状态）、个股仓位公式、加减清仓规则 |
| 铁律 | 7 条零容忍规则，含违规后果 |
| 情绪波动 | 5 种情绪（贪婪/恐惧/后悔/希望/疲惫）的触发场景与应对方法 |
| 每日流程 | 盘前(8:30-9:25)、盘中(9:30-15:00)、盘后(15:00+)的详细操作清单 |
| 系统失效 | 5 项失效指标 |
| 核心公式 | 长期盈利 = 正期望策略 × 严格风控 × 纪律执行 × 时间复利 |

---

## 4. 后端接口规范

### 4.1 基础信息

- 基地址: `http://localhost:3001/api`
- 开发代理: Vite 将 `/api` 代理到 `localhost:3001`
- 所有接口返回 JSON，格式: `{ ok: boolean, data?: any, error?: string }`
- 数据来源: 东方财富公开 API，后端添加 Referer 和 User-Agent 头

### 4.2 接口清单

#### 市场数据

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/market/indices` | — | 上证/深证/创业板指数，含 120 日 K 线、MA20/60/120、MA60 四日趋势 |
| GET | `/market/breadth` | — | 沪深两市上涨/下跌/平盘家数 |
| GET | `/market/indices/intraday` | — | 三大指数当日 1 分钟分时数据（含昨收价） |
| GET | `/market/northbound` | — | 近 5 日北向资金净流入 |

#### 个股数据

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `/stock/:code/kline` | — | 120 日日 K 线（含 prevClose，用于 ATR 计算） |
| GET | `/stock/:code/intraday` | — | 当日 1 分钟分时数据（含昨收价） |
| GET | `/stock/:code/basic` | — | PE、PB、市值等基本面指标 |
| GET | `/stock/:code/kline5y` | — | ~1200 日日 K 线（约 5 年） |
| GET | `/stock/batch/quotes` | `?codes=xxx,yyy` | 批量实时行情（含名称、价、涨跌幅、成交额） |
| GET | `/stock/search` | `?kw=关键词` | 股票搜索（支持代码/名称/拼音），仅返回 A 股 |

#### 系统

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |

### 4.3 数据格式

#### K 线对象
```json
{ "date": "2026-05-12", "open": 10.5, "close": 10.8, "high": 11.0, "low": 10.3, "volume": 50000, "amount": 540000 }
```

#### 分时数据点
```json
{ "time": "09:31", "close": 10.8, "avg": 10.75, "volume": 500 }
```

#### 批量行情
```json
{ "000001": { "name": "平安银行", "close": 12.5, "change": 1.23, "changeAmt": 0.15, "high": 12.8, "low": 12.3, "amount": 560000000 } }
```
注意: `change` 为涨跌幅百分比 × 100（如 `123` 代表 1.23%），前端需除以 100 显示。

---

## 5. 数据模型

### 5.1 持久化数据（localStorage）

| Key | 类型 | 说明 |
|-----|------|------|
| `watchlist` | Stock[] | 自选股列表 |
| `holdings` | Holding[] | 当前持仓 |
| `totalCapital` | Number | 总资金（默认 1,000,000） |
| `journal_trades` | Trade[] | 交易记录 |

### 5.2 核心数据结构

#### Stock（自选股）
```typescript
interface Stock {
  code: string       // 股票代码，如 "000001"
  name: string       // 股票名称
  addedAt: number    // 添加时间戳
}
```

#### Holding（持仓）
```typescript
interface Holding {
  id: number            // Date.now()
  code: string
  buyPrice: number
  stopPrice: number     // 初始止损价
  trailingStop: number  // 跟踪止盈价（只能上移）
  position: number      // 持仓金额
  strategy: string      // 'trend' | 'pullback' | 'bottom'
  strategyName: string
  industry: string
  date: string          // 开仓日期
}
```

#### Trade（交易记录）
```typescript
interface Trade {
  id: number
  code: string
  name: string
  strategy: string
  strategyName: string
  buyPrice: number
  quantity: number
  stopPrice: number
  targetPrice: number
  atr: number
  atrN: number
  marketRegime: string    // 'bull' | 'bull-lean' | 'neutral' | 'bear-lean' | 'bear'
  industry: string
  date: string
  checklist: boolean[]    // 9 项
  status: 'open' | 'closed'
  // 以下平仓后填充
  sellPrice: number | null
  sellDate: string | null
  sellReason: 'stop' | 'target' | 'trailing_stop' | 'time_stop' | 'logic_stop' | 'manual' | null
  pnl: number | null
  pnlPct: number | null
  actualRR: number | null
  // 复盘数据
  emotion: number | null       // 1-5
  executionScore: number | null // 1-5
  violations: number[]         // 铁律 ID
  issues: string[]             // 问题标签 key
  reviewNotes: string
}
```

### 5.3 运行时缓存（不持久化）

| Store | 缓存内容 | 说明 |
|-------|----------|------|
| market | indices, breadth, northbound | 每次进入大盘页面重新获取 |
| watchlist | quotes, klineCache | 30 秒刷新行情 |
| watchlist (Watchlist.vue 内) | basicCache, intradayCache, kline5yCache | 按需加载 |

---

## 6. 公共组件

### 6.1 Sparkline 迷你走势图

| Prop | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| data | Array | — | K 线数组 `[{ close }]` |
| positive | Boolean | — | true 绿色，false 红色 |
| width | Number | 52 | 固定宽度（px） |
| height | Number | 24 | 高度（px） |
| showArea | Boolean | false | 是否显示面积填充 |
| refPrice | Number | — | 参考价虚线（如昨收价） |
| autoWidth | Boolean | false | 响应式宽度（CSS 100%） |

**特性**:
- Canvas 渲染，支持 Retina 高清（devicePixelRatio 缩放）
- `autoWidth` 模式使用 ResizeObserver 自动适配容器宽度
- 绿色 `#30d158` / 红色 `#ff453a`
- `showArea` 模式附带渐变面积填充 + 高低价标注 + 参考价标注

### 6.2 NavBar 导航栏

- 顶部 sticky，z-index 100
- 背景 `var(--surface)`，底部 1px 边框
- Logo 「SmartStock」蓝色加粗
- 6 个 router-link，激活态蓝色背景 + 蓝色文字

---

## 7. 交易规则体系（核心业务逻辑）

### 7.1 六维大盘判定算法

来源: `src/utils/marketJudge.js`

输入: 指数数据（MA、K 线、涨跌家数、北向资金）

输出:
```typescript
{
  status: 'bull' | 'bull-lean' | 'neutral' | 'bear-lean' | 'bear'
  label: string       // 如「牛市」「偏多」
  tag: string         // CSS class
  maxPosition: string // 如 '80-100%'
  strategy: string    // 推荐策略
  signals: object[]   // 各维度判断结果
  score: { bull, bear, neutral } // 信号计数
  confirmed: boolean  // ≥ 3 个同向信号
}
```

### 7.2 仓位计算公式

来源: `src/utils/position.js`

```
止损距离 = N × ATR(14)
止损价 = 买入价 − 止损距离
止损比例 = 止损距离 / 买入价
风险预算 = 总资金 × 2%
单笔仓位 = min(风险预算 / 止损比例, 总资金 × 25%)
盈亏比 = (目标价 − 买入价) / 止损距离
跟踪止盈初始价 = 买入价 × (1 − 回撤比例)
```

### 7.3 ATR 计算方法

```
True Range = max(当日最高 − 当日最低, |当日最高 − 昨收|, |当日最低 − 昨收|)
ATR(14) = SMA(TR, 14)  // 取最近 15 根 K 线，计算后 14 个 TR 的均值
```

---

## 8. 非功能性需求

### 8.1 性能

- 股票池行情 30 秒自动刷新，批量查询
- Sparkline 使用 Canvas 渲染，避免 DOM 开销
- K 线数据客户端缓存（klineCache），避免重复请求
- 搜索输入 300ms 防抖

### 8.2 兼容性

- 现代浏览器（Chrome / Edge / Firefox / Safari）
- 移动端响应式适配（768px 断点）
- Retina 屏幕 DPR 适配（Canvas）

### 8.3 数据安全

- 所有用户数据（自选、持仓、交易记录）仅存储在浏览器 localStorage
- 无服务端数据库，无用户注册/登录
- API 仅为东方财富公开数据的代理转发

### 8.4 可用性

- 无网络时：已缓存的自选列表和交易记录仍可查看（离线只读）
- API 失败时：静默降级，不阻塞页面操作
- 删除操作需二次确认

---

## 9. 信息架构图

```
SmartStock
├── 大盘状态 (Dashboard)
│   ├── 六维判据表
│   ├── 综合判定结果
│   ├── 长窗口速判
│   └── 交易前检查清单
├── 股票池 (Watchlist)
│   ├── 指数分时栏 (上证/深证/创业板)
│   ├── 自选股列表 (搜索/添加/删除)
│   └── 个股详情 (走势图/基本面/操作)
├── 选股筛选 (Screener)
│   ├── 排雷清单 (10项)
│   ├── 基本面参数 (7项)
│   ├── 景气度选择 (3选1)
│   ├── 技术信号选择 (3选1)
│   └── 筛选条件输出 (自然语言 + 公式)
├── 仓位计算 (Position)
│   ├── ATR 动态计算器
│   ├── 行业集中度
│   ├── 持仓管理表
│   └── 策略参数参考
├── 交易日志 (Journal)
│   ├── 交易记录 (新增/平仓/查看)
│   ├── 绩效统计 (胜率/盈亏比/月度曲线)
│   └── 复盘分析 (违规统计/系统警告)
└── 策略速查 (Guide)
    ├── 买入决策树
    ├── 买入策略
    ├── 卖出规则
    ├── 仓位管理
    ├── 铁律
    ├── 情绪管理
    ├── 每日流程
    ├── 系统失效指标
    └── 核心公式
```

---

## 10. 页面流转

```
大盘判断(合理) → 选股筛选 → 加入自选 → 股票池确认 → 仓位计算 → 记录交易
     ↓               ↓            ↓             ↓            ↓          ↓
  Dashboard      Screener    Watchlist     Watchlist    Position    Journal
                                                                    ↓
                                                              绩效统计 + 复盘
```

**典型操作路径**:
1. 打开「大盘状态」→ 确认市场环境适合交易
2. 进入「选股筛选」→ 通过四层漏斗筛选候选股
3. 将候选股加入「股票池」→ 查看走势和基本面
4. 确认后进入「仓位计算」→ 计算 ATR、止损、仓位
5. 在「交易日志」新增交易记录
6. 平仓时填写复盘数据 → 在「绩效统计」和「复盘分析」中查看结果
7. 随时可查「策略速查」确认规则细节
